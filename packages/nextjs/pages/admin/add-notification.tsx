import type { NextPage } from "next";
import { MetaHeader } from "~~/components/MetaHeader";

const AddNotification: NextPage = () => {
  return (
    <div>
      <MetaHeader />
    </div>
  );
};

export default AddNotification;
